import {createTheme} from '@mui/material/styles'

export const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(0,212,255)',
            dark: 'rgb(81,62,183)'
        },
        secondary:{
            main: '#FFF',
            dark: '#EBF2F4'
        }
    }
})